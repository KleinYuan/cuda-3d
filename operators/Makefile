uniform_sample:
	nvcc -o uniform_sample uniform_sample.cu -run

euclidean_distance:
	nvcc -o euclidean_distance euclidean_distance.cu -run

chamfer_distance:
	nvcc -o chamfer_distance chamfer_distance.cu -run
